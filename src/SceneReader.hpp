#pragma once
#include <vector>
#include <string>

/**
 * @brief Initializes a string from a .json file in the param
 * 
 * @param fPath The path to the .json file
 */
void InitSceneFromFile(const std::string);